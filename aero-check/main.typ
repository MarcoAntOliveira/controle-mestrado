#import "@preview/aero-check:0.1.1": *

#show: checklist.with(
  title: "Trabalho controle mestrado",
  // disclaimer: "",
  // style: 1,
)

#topic("Seção 0)")[
  #section("Parametros")[
    #step("obter os parametros", "Check")\
  ]
    #section("Section")[
    #step("Obtenha o diagrama de blocos do sistema", "uncheck")\
    #step("Obtenha as funções de transferência da tensão de estator para a velocidade, e do torque de carga para a velocidade;", "uncheck")\
    #step("Obtenha as respostas ao degrau para cada uma dessas entradas;", "uncheck")\
    #step("Analise e comente os resultados anteriores.", "uncheck")\
  ]

]

#topic("Seção 3")[
  #section("Representação em Espaço de Estados")[
    #step("Obtenha uma representação em espaço de estados para o sistema apresentado. Analise os polos e zeros desta representação;", "uncheck")\


      #step("Obtenha uma representação em espaço de estados para o sistema apresentado. Analise os polos e zeros desta representação;", "uncheck")\
      #step("Escolha um tempo de amostragem e discretize a representação de espaço de estados. Simule e mostre a adequação desta discretização face o sistema contínuo;", "uncheck")\
      #step("Analise a controlabilidade e observabilidade da representação em espaço de estados;", "uncheck")\
      #step("Proponha e realize um controlador por realimentação estática de estados;", "uncheck")\
      #step("Supondo que os estados da planta não sejam mensuráveis, proponha um observador de estados para a mesma.", "uncheck")\
   ]
]
