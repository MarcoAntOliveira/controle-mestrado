#import "@preview/aero-check:0.1.1": *

#show: checklist.with(
  title: "Conops",
  // disclaimer: "",
  // style: 1,
)

#topic("Topic")[
  #section("Section")[
    #step("Step", "Check")
  ]
    #section("Section")[
    #step("Step", "Check")
    #step("Step", "Check")
    #step("Step", "Check")
    #step("Step", "Check")
  ]
    #section("Section")[
    #step("Step", "Check")
  ]
]

#topic("Topic")[
  #section("Section")[
    #step("Step", "Check")
  ]
]
